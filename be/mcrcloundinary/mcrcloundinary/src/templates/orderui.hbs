<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Order Confirmation</title>
    <style>
      /* Reset */
      img {
        max-width: 100%;
        height: auto;
      }
      table {
        border-collapse: collapse;
      }
      /* Responsive */
      @media only screen and (max-width: 600px) {
        .container {
          width: 100% !important;
        }
        .mobile-center {
          text-align: center !important;
        }
        .product-table th, 
        .product-table td {
          display: block;
          width: 100% !important;
          text-align: left !important;
        }
        .product-table img {
          margin-bottom: 10px;
        }
      }
    </style>
  </head>
  <body style="font-family: Arial, sans-serif; background-color:#f6f6f6; margin:0; padding:20px;">
    <table align="center" width="600" class="container" cellpadding="0" cellspacing="0" style="background:#ffffff; border-radius:8px; overflow:hidden; max-width:600px;">
      <!-- Header -->
      <tr>
        <td style="background:#4CAF50; color:white; padding:20px; text-align:center; font-size:20px;">
          Cảm ơn bạn đã đặt hàng!
        </td>
      </tr>

      <!-- Customer Info -->
      <tr>
        <td style="padding:20px;">
          <h3>Thông tin khách hàng</h3>
          <p><strong>Tên:</strong> {{customerName}}</p>
          <p><strong>Email:</strong> {{customerEmail}}</p>
          <p><strong>Ngày đặt:</strong> {{orderDate}}</p>
        </td>
      </tr>

      <!-- Product Table -->
      <tr>
        <td style="padding:20px;">
          <h3>Chi tiết đơn hàng</h3>
          <table class="product-table" width="100%" cellpadding="10" cellspacing="0" border="1" style="border-collapse:collapse; text-align:left;">
            <thead style="background:#f0f0f0;">
              <tr>
                <th>Ảnh</th>
                <th>Tên sản phẩm</th>
                <th>Giá</th>
              </tr>
            </thead>
            <tbody>
              {{#each products}}
              <tr>
                <td style="text-align:center;">
                  <img src="{{this.image}}" width="80" style="border-radius:4px;" />
                </td>
                <td>{{this.name}}</td>
                <td style="white-space:nowrap;">{{this.price}}</td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </td>
      </tr>

      <!-- Total -->
      <tr>
        <td style="padding:20px; text-align:right; font-size:18px;">
          <strong>Tổng cộng: {{totalPrice}}</strong>
        </td>
      </tr>

      <!-- Button -->
      <tr>
        <td style="padding:20px; text-align:center;">
          <a href="{{redirectUrl}}" 
             style="display:inline-block; padding:14px 28px; background:#4CAF50; color:#fff; text-decoration:none; border-radius:5px; font-weight:bold; font-size:16px;">
            Xem đơn hàng
          </a>
        </td>
      </tr>

      <!-- Footer -->
      <tr>
        <td style="background:#f0f0f0; padding:15px; text-align:center; font-size:12px; color:#666;">
          © 2025 Your Shop. All rights reserved.
        </td>
      </tr>
    </table>
  </body>
</html>
